{
  "name": "massed-compute-mcp",
  "version": "1.1.0",
  "description": "Stdio bridge to the hosted Massed Compute MCP endpoint — a verbatim JSON-RPC pass-through.",
  "license": "MIT",
  "author": "Massed Compute",
  "type": "module",
  "mcpName": "io.github.Massed-Compute/mcp",
  "main": "dist/cli.js",
  "bin": {
    "massed-compute-mcp": "./dist/cli.js"
  },
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts",
    "dist/**/*.json",
    "README.md",
    "LICENSE"
  ],
  "engines": { "node": ">=20" },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "massed-compute",
    "gpu",
    "vm",
    "claude",
    "anthropic",
    "ai"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Massed-Compute/massed-compute-mcp.git"
  },
  "homepage": "https://github.com/Massed-Compute/massed-compute-mcp#readme",
  "bugs": {
    "url": "https://github.com/Massed-Compute/massed-compute-mcp/issues"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && cp ../../README.md README.md && cp ../../LICENSE LICENSE && chmod +x dist/cli.js",
    "pretest": "npm run build",
    "prepublishOnly": "npm run build && npm test",
    "start": "node dist/cli.js server",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^1"
  }
}
