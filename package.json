{
  "name": "@massed-compute/mcp-monorepo",
  "private": true,
  "description": "Massed Compute MCP wrapper — monorepo root. Publishes packages/node (npm) and packages/python (pip), each a verbatim JSON-RPC pass-through to the hosted MCP endpoint.",
  "scripts": {
    "sync-docs": "node scripts/sync-docs.mjs",
    "build:node": "npm --prefix packages/node run build",
    "test:node": "npm --prefix packages/node test",
    "test:python": "cd packages/python && .venv/bin/pytest tests/",
    "test:smoke": "node scripts/smoke-test.mjs",
    "test": "npm run sync-docs && npm run test:node && npm run test:python && npm run test:smoke"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
